-r base.txt
gunicorn>=22.0
# The ASGI worker class gunicorn loads (ADR-017, IR-156). Without it the
# production CMD fails at startup: 'class uri ... invalid or not found'.
uvicorn[standard]>=0.29.0
sentry-sdk>=2.0
# TODO: add django-storages S3 config in production settings
