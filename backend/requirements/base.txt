Django>=5.0,<5.2
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-filter>=24.1
django-axes>=6.4
psycopg2-binary>=2.9
celery[redis]>=5.3
redis>=5.0

# pgvector: FR-M3-03, vector storage in PostgreSQL (VectorField + HNSW index).
# Pinned once. A second, weaker pin (>=0.2.4) sat further down this file and was
# removed 2026-09-17 — two pins for one package is how they drift apart.
pgvector>=0.3

# Extraction is one on-premise service (ADR-016): Docling-serve over HTTP. The
# extractor libraries the prototype chain imported — unstructured, pymupdf,
# pytesseract — were never declared here and are gone with it.

# openai: the client for every OpenAI-compatible vendor (Groq, OpenRouter, etc.),
# not only OpenAI — apps/ai/providers/openai_compatible.py imports it directly
# (ADR-021). A 2026-09-17 removal claimed nothing under backend/ imported it;
# that was false and broke the configured-LLM test path. Restored.
openai>=1.30

# httpx: Docling-serve extraction (ADR-016), Voyage embedding and reranking
# (ADR-015), and the OpenAI-compatible inference call (ADR-021). It no longer
# proxies to the ai-gateway — ADR-024 took the embedding path off it, and nothing
# else in Django called it.
httpx>=0.27.0

# IR-287: the chunker counts real voyage-context-4 tokens. This is HuggingFace's
# BPE engine, not a vendor SDK — the vocabulary itself is vendored at
# apps/ai/chunking/tokenizer/voyage-context-4.json and pinned by SHA-256, so
# nothing is downloaded at build or run time.
#
# Exactly pinned, alone in this file, and deliberately: the argument for
# replacing the old whitespace counter was that a real tokenizer is
# *deterministic across processes and versions*, and a range does not deliver
# that — any release free to change a count is free to re-chunk and re-embed
# the whole corpus. Both halves are pinned or neither claim holds. Moving it
# is a deliberate act with a diff, and re-chunking is its cost.
tokenizers==0.22.2
pyexcel-xls>=0.7
pyexcel-xlsx>=0.6
openpyxl>=3.1                  # FR-M2-04: styled .xlsx template download
python-docx>=1.1
python-decouple>=3.8
django-storages[s3]>=1.14
whitenoise>=6.6
