import { ComingSoonPage } from "@/components/shared/ComingSoonPage";

export default function StoragePage() {
  return (
    <ComingSoonPage
      title="Storage"
      description="Institutional file storage — create folders, upload files, and share them across your team — is being prepared and will be available soon."
      icon="fa-folder-open"
      backTo="/"
      backLabel="Return to Discover"
    />
  );
}
